<?php

namespace App\Manager;

class FuelPriceManager {

    /**
     * 
     */
    public function checkFields(array $jsonContent) {
        $fields = [];

        foreach($jsonContent as $fieldName => $fieldValue) {
            // 
        }

        return $fields;
    }

    /**
     * 
     */
    public function fillFuelPrice(array $fields/*, ?FuelPrice $fuelPrice = new FuelPrice()*/) {
        // 
    }
}